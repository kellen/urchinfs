urchin-fs
=========

D implementation.

## Debian/Ubuntu

Add [d-apt](http://d-apt.sourceforge.net/) sources:

```
sudo wget http://master.dl.sourceforge.net/project/d-apt/files/d-apt.list \
          -O /etc/apt/sources.list.d/d-apt.list
sudo apt-get update 
sudo apt-get -y --allow-unauthenticated install --reinstall d-apt-keyring 
sudo apt-get update
```

Verify public key matches `0xEBCF975E5BA24D5E`

Install fuse and dmd:

```
sudo apt-get install \
     fuse libfuse2 libfuse-dev \
     dmd-bin
```
