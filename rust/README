urchin-fs
---------

Rust implementation.

## Build

### Prerequiresite

`rust-fuse` requires pkg-config and the fuse development files:

```
sudo apt-get install pkg-config fuse libfuse-dev
```

### Build

```
cargo build
```

### Debugging

To get a stacktrace: `export RUST_BACKTRACE=1`

To set logging level: `export RUST_LOG=urchinfs`
